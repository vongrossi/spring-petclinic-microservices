version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk
      
      
      # run tests!
      - run: mvn clean install -PbuildDocker -DskipTests
      
      
